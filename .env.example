# MongoDB Atlas Connection String
# Replace <username>, <password>, and <cluster-url> with your MongoDB Atlas details.
# The default database is 'media_scoring'.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/media_scoring?retryWrites=true&w=majority
